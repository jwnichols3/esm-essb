package com.bgi.esm.monitoring.platform.test.openview.orm;

import org.apache.log4j.Logger;
import com.bgi.esm.monitoring.platform.test.openview.orm.OpenviewMessageText;

/**
 * @author Dennis Lin (dept=Enterprise Systems Management, username=linden)
 */
public class ActiveMessageText extends OpenviewMessageText
{
    final private static Logger _log = Logger.getLogger ( ActiveMessageText.class );

    public OpenviewMessageText getValue()
    {
        ActiveMessageText message_text = new ActiveMessageText();
        message_text.setValue ( this );

        return message_text;
    }
};
