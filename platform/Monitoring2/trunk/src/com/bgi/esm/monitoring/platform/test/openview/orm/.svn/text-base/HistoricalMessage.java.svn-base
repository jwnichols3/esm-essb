package com.bgi.esm.monitoring.platform.test.openview.orm;

import org.apache.log4j.Logger;
import com.bgi.esm.monitoring.platform.test.openview.orm.OpenviewMessage;

/**
 * @author Dennis Lin (dept=Enterprise Systems Management, username=linden)
 */
public class HistoricalMessage extends OpenviewMessage
{
    final private static Logger _log = Logger.getLogger ( HistoricalMessage.class );

    public HistoricalMessage getValue()
    {
        HistoricalMessage message = new HistoricalMessage();
        message.setValue ( this );

        return message;
    }
};
