create table monotonic (row_id int primary key, current_value int not null);
insert into monotonic values(1, 1);

create table monotonic_datamap (row_id int primary key, current_value int not null);
insert into monotonic_datamap values(1, 1);

create table monotonic_suppression (row_id int primary key, current_value int not null);
insert into monotonic_suppression values(1, 1);

create table monotonic_throttle (row_id int primary key, current_value int not null);
insert into monotonic_throttle values(1, 1);
